name: my_wellness
description: Private preventive-health companion for My Wellness members.
publish_to: none
version: 1.0.1+2

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  json_annotation: ^4.9.0 
  # UI and asset management
  google_fonts: ^6.2.1
  shimmer: ^3.0.0
  cached_network_image: ^3.2.3
  flutter_animate: ^4.5.2
  provider: ^6.1.2
  image_picker: ^1.1.2
  local_auth: ^2.3.0
  flutter_staggered_grid_view: ^0.7.0
  url_launcher: ^6.3.0
  map_launcher: ^3.5.0
  geolocator: ^14.0.2 
  geocoding: ^4.0.0 
  intl: ^0.20.2
  flutter_image_compress: ^2.1.0
  path: ^1.9.0
  path_provider: ^2.1.2
  camera: ^0.11.0
  video_player: ^2.8.6
  chewie: ^1.8.5
  google_maps_flutter: ^2.10.0
  permission_handler: ^12.0.1
  
  # State Management, DI, and Architecture
  flutter_bloc: ^9.1.1
  equatable: ^2.0.5
  dartz: ^0.10.1
  get_it: ^8.3.0
  injectable: ^2.1.2
  auto_route: ^11.1.0
  shared_preferences: ^2.5.3
  bloc_concurrency: ^0.3.0
  
  # Networking and Security
  dio: ^5.4.3+1
  flutter_secure_storage: ^10.0.0
  flutter_dotenv: ^6.0.0
  flashy_tab_bar2: ^0.0.10
  webview_flutter: ^4.13.0
  flutter_contacts: ^1.1.9+1
  package_info_plus: ^9.0.0
  file_picker: ^8.1.7
  font_awesome_flutter: ^11.0.0
  flutter_tts: ^4.2.5
  purchases_flutter: ^9.15.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.4
  
  
  # Code Generators (dev_dependencies)
  build_runner: ^2.4.9
  injectable_generator: ^2.6.2
  auto_route_generator: ^10.4.0
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
  assets:
    - env/
    - assets/images/
    - assets/lang/en.json
    - assets/lang/sw.json
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/AppIcons (21)/android/playstore-icon.png"
  remove_alpha_ios: true
  adaptive_icon_background: "#008080"
  adaptive_icon_foreground: "assets/AppIcons (21)/android/playstore-icon.png"
  min_sdk_android: 21