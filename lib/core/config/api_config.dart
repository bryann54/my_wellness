import 'package:injectable/injectable.dart';

/// Deliberately blank until an approved environment/API contract supplies it.
@lazySingleton
class ApiConfig {
  const ApiConfig();

  String get baseUrl => '';
}
